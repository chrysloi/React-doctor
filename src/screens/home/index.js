import { View, Text, SafeAreaView } from "react-native";

const Home = () => {
  return (
    <SafeAreaView style={{paddingTop:30}}>
      <View>
        <Text>I am Home</Text>
      </View>
    </SafeAreaView>
  );
};

export default Home;
