import { View, Text, SafeAreaView } from "react-native";

const Consult = () => {
  return (
    <SafeAreaView style={{paddingTop:30}}>
      <View>
        <Text>I am Consult</Text>
      </View>
    </SafeAreaView>
  );
};

export default Consult;
